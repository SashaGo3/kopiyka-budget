/**
 * Spanish. Keys are the English source string; see `i18n/index.ts`.
 * An array value is a plural set in the order `tn` expects. Dos formas de plural: una / varias.
 */
import type { Catalog } from "./index";

export const es: Catalog = {
};
