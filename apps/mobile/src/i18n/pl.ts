/**
 * Polish. Keys are the English source string; see `i18n/index.ts`.
 * An array value is a plural set in the order `tn` expects. Trzy formy liczby mnogiej: jedna / kilka / wiele.
 */
import type { Catalog } from "./index";

export const pl: Catalog = {
};
